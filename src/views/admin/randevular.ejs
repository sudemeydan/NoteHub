<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css?v=<%= Date.now() %>">
    <style>
        /* Randevu sayfasına özel stiller */
        .badge { padding: 5px 10px; border-radius: 12px; font-size: 0.85rem; font-weight: bold; color: white; }
        .bg-pending { background-color: #ffc107; color: #000; }
        .bg-confirmed { background-color: #198754; }
        .bg-rejected { background-color: #6c757d; }
        .bg-busy { background-color: #dc3545; }
        .btn-approve { background-color: #198754; color: white; margin-right: 5px; }
        .btn-reject { background-color: #ffc107; color: black; margin-right: 5px; }
        .btn-delete { background-color: #dc3545; color: white; }
        .section-header-confirmed { border-left: 5px solid #198754; padding-left: 10px; color: #198754; margin-bottom: 15px; }
        .section-header-pending { border-left: 5px solid #ffc107; padding-left: 10px; color: #d39e00; margin-bottom: 15px; }
    </style>
</head>
<body>

    <div class="admin-wrapper">
        <%- include('partials/sidebar', { path: '/admin/randevular' }) %>

        <div class="admin-content">
            <div class="admin-topbar">
                <h4 class="mb-0 text-dark">Randevu Yönetimi</h4>
                <div class="user-info"><strong class="text-dark"><%= currentUser.username %></strong></div>
            </div>

            <div class="admin-main-container">
                <%- include('../partials/messages') %>

                <div class="row">
                    <div class="col-md-4 order-md-2 mb-4">
                        <div class="admin-card">
                            <div class="admin-card-header bg-light">
                                <h3><i class="bi bi-calendar-x text-danger"></i> Saat Bloke Et</h3>
                            </div>
                            <div class="card-body p-4">
                                <form action="/admin/randevular/create" method="POST">
                                    <div class="mb-3">
                                        <label class="form-label fw-bold">Tarih</label>
                                        <input type="date" class="form-control" name="date" required>
                                    </div>
                                    <div class="row">
                                        <div class="col-6 mb-3">
                                            <label class="form-label fw-bold">Başlangıç</label>
                                            <input type="time" class="form-control" name="startTime" required>
                                        </div>
                                        <div class="col-6 mb-3">
                                            <label class="form-label fw-bold">Bitiş</label>
                                            <input type="time" class="form-control" name="endTime" required>
                                        </div>
                                    </div>
                                    <div class="d-grid">
                                        <button type="submit" class="btn btn-danger">Meşgul Olarak İşaretle</button>
                                    </div>
                                </form>
                            </div>
                        </div>
                        
                        <div class="admin-card mt-3">
                            <div class="admin-card-header">
                                <h3>Bloklarınız</h3>
                            </div>
                            <ul class="list-group list-group-flush">
                                <% if (busySlots && busySlots.length > 0) { %>
                                    <% busySlots.forEach(slot => { %>
                                        <li class="list-group-item d-flex justify-content-between align-items-center">
                                            <span>
                                                <strong><%= new Date(slot.startTime).toLocaleDateString('tr-TR') %></strong><br>
                                                <%= new Date(slot.startTime).toLocaleTimeString('tr-TR', {hour:'2-digit', minute:'2-digit'}) %> - 
                                                <%= new Date(slot.endTime).toLocaleTimeString('tr-TR', {hour:'2-digit', minute:'2-digit'}) %>
                                            </span>
                                            <form action="/admin/randevular/delete" method="POST" onsubmit="return confirm('Kaldırmak istiyor musunuz?');" class="m-0">
                                                <input type="hidden" name="appointmentId" value="<%= slot.id %>">
                                                <button type="submit" class="btn btn-sm btn-outline-secondary"><i class="bi bi-trash"></i></button>
                                            </form>
                                        </li>
                                    <% }) %>
                                <% } else { %>
                                    <li class="list-group-item text-muted text-center">Bloke saat yok.</li>
                                <% } %>
                            </ul>
                        </div>
                    </div>

                    <div class="col-md-8 order-md-1">
                        <div class="admin-card mb-4 border-success border-start border-5">
                            <div class="admin-card-header">
                                <h3 class="text-success">Onaylanan Randevular</h3>
                            </div>
                            <div class="card-body p-0">
                                <% const confirmedApps = studentAppointments.filter(app => app.status === 'confirmed'); %>
                                <% if (confirmedApps.length > 0) { %>
                                    <table class="table table-hover mb-0">
                                        <thead class="table-light">
                                            <tr>
                                                <th>Tarih</th>
                                                <th>Öğrenci</th>
                                                <th>Konu</th>
                                                <th>İşlem</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% confirmedApps.forEach(app => { %>
                                                <tr>
                                                    <td>
                                                        <%= new Date(app.startTime).toLocaleDateString('tr-TR') %> <br>
                                                        <small><%= new Date(app.startTime).toLocaleTimeString('tr-TR', {hour:'2-digit', minute:'2-digit'}) %> - <%= new Date(app.endTime).toLocaleTimeString('tr-TR', {hour:'2-digit', minute:'2-digit'}) %></small>
                                                    </td>
                                                    <td><%= app.Student ? app.Student.username : '?' %></td>
                                                    <td><%= app.studentNotes %></td>
                                                    <td>
                                                        <form action="/admin/randevular/delete" method="POST" onsubmit="return confirm('İptal etmek istiyor musunuz?');">
                                                            <input type="hidden" name="appointmentId" value="<%= app.id %>">
                                                            <button class="btn btn-sm btn-delete">İptal</button>
                                                        </form>
                                                    </td>
                                                </tr>
                                            <% }) %>
                                        </tbody>
                                    </table>
                                <% } else { %>
                                    <p class="p-3 text-center text-muted">Onaylanmış randevu yok.</p>
                                <% } %>
                            </div>
                        </div>

                        <div class="admin-card border-warning border-start border-5">
                            <div class="admin-card-header">
                                <h3 class="text-warning">Bekleyen Talepler</h3>
                            </div>
                            <div class="card-body p-0">
                                <% const pendingApps = studentAppointments.filter(app => app.status === 'pending'); %>
                                <% if (pendingApps.length > 0) { %>
                                    <table class="table table-hover mb-0">
                                        <thead class="table-light">
                                            <tr>
                                                <th>Tarih</th>
                                                <th>Öğrenci</th>
                                                <th>Mesaj</th>
                                                <th>İşlemler</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% pendingApps.forEach(req => { %>
                                                <tr>
                                                    <td>
                                                        <%= new Date(req.startTime).toLocaleDateString('tr-TR') %> <br>
                                                        <small><%= new Date(req.startTime).toLocaleTimeString('tr-TR', {hour:'2-digit', minute:'2-digit'}) %></small>
                                                    </td>
                                                    <td><%= req.Student ? req.Student.username : '?' %></td>
                                                    <td><small><%= req.studentNotes %></small></td>
                                                    <td>
                                                        <div class="d-flex gap-1">
                                                            <form action="/admin/randevular/confirm" method="POST">
                                                                <input type="hidden" name="appointmentId" value="<%= req.id %>">
                                                                <button class="btn btn-sm btn-approve"><i class="bi bi-check-lg"></i></button>
                                                            </form>
                                                            <form action="/admin/randevular/reject" method="POST">
                                                                <input type="hidden" name="appointmentId" value="<%= req.id %>">
                                                                <button class="btn btn-sm btn-reject"><i class="bi bi-x-lg"></i></button>
                                                            </form>
                                                        </div>
                                                    </td>
                                                </tr>
                                            <% }) %>
                                        </tbody>
                                    </table>
                                <% } else { %>
                                    <p class="p-3 text-center text-muted">Bekleyen talep yok.</p>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>